schema {
  query: Query
}

type Query {
  orders: [Order!]!
  order(id: UUID!): Order
}

type Order {
  id: UUID!
  name: String!
  price: Decimal!
}

scalar UUID

"The built-in `Decimal` scalar type."
scalar Decimal